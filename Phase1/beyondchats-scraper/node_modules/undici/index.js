'use strict'

const Client = require('undici/lib/dispatcher/client')
const Dispatcher = require('undici/lib/dispatcher/dispatcher')
const Pool = require('undici/lib/dispatcher/pool')
const BalancedPool = require('undici/lib/dispatcher/balanced-pool')
const Agent = require('undici/lib/dispatcher/agent')
const ProxyAgent = require('undici/lib/dispatcher/proxy-agent')
const EnvHttpProxyAgent = require('undici/lib/dispatcher/env-http-proxy-agent')
const RetryAgent = require('undici/lib/dispatcher/retry-agent')
const H2CClient = require('undici/lib/dispatcher/h2c-client')
const errors = require('undici/lib/core/errors')
const util = require('undici/lib/core/util')
const { InvalidArgumentError } = errors
const api = require('undici/lib/api')
const buildConnector = require('undici/lib/core/connect')
const MockClient = require('undici/lib/mock/mock-client')
const { MockCallHistory, MockCallHistoryLog } = require('undici/lib/mock/mock-call-history')
const MockAgent = require('undici/lib/mock/mock-agent')
const MockPool = require('undici/lib/mock/mock-pool')
const SnapshotAgent = require('undici/lib/mock/snapshot-agent')
const mockErrors = require('undici/lib/mock/mock-errors')
const RetryHandler = require('undici/lib/handler/retry-handler')
const { getGlobalDispatcher, setGlobalDispatcher } = require('undici/lib/global')
const DecoratorHandler = require('undici/lib/handler/decorator-handler')
const RedirectHandler = require('undici/lib/handler/redirect-handler')

Object.assign(Dispatcher.prototype, api)

module.exports.Dispatcher = Dispatcher
module.exports.Client = Client
module.exports.Pool = Pool
module.exports.BalancedPool = BalancedPool
module.exports.Agent = Agent
module.exports.ProxyAgent = ProxyAgent
module.exports.EnvHttpProxyAgent = EnvHttpProxyAgent
module.exports.RetryAgent = RetryAgent
module.exports.H2CClient = H2CClient
module.exports.RetryHandler = RetryHandler

module.exports.DecoratorHandler = DecoratorHandler
module.exports.RedirectHandler = RedirectHandler
module.exports.interceptors = {
  redirect: require('undici/lib/interceptor/redirect'),
  responseError: require('undici/lib/interceptor/response-error'),
  retry: require('undici/lib/interceptor/retry'),
  dump: require('undici/lib/interceptor/dump'),
  dns: require('undici/lib/interceptor/dns'),
  cache: require('undici/lib/interceptor/cache'),
  decompress: require('undici/lib/interceptor/decompress')
}

module.exports.cacheStores = {
  MemoryCacheStore: require('undici/lib/cache/memory-cache-store')
}

const SqliteCacheStore = require('undici/lib/cache/sqlite-cache-store')
module.exports.cacheStores.SqliteCacheStore = SqliteCacheStore

module.exports.buildConnector = buildConnector
module.exports.errors = errors
module.exports.util = {
  parseHeaders: util.parseHeaders,
  headerNameToString: util.headerNameToString
}

function makeDispatcher (fn) {
  return (url, opts, handler) => {
    if (typeof opts === 'function') {
      handler = opts
      opts = null
    }

    if (!url || (typeof url !== 'string' && typeof url !== 'object' && !(url instanceof URL))) {
      throw new InvalidArgumentError('invalid url')
    }

    if (opts != null && typeof opts !== 'object') {
      throw new InvalidArgumentError('invalid opts')
    }

    if (opts && opts.path != null) {
      if (typeof opts.path !== 'string') {
        throw new InvalidArgumentError('invalid opts.path')
      }

      let path = opts.path
      if (!opts.path.startsWith('/')) {
        path = `/${path}`
      }

      url = new URL(util.parseOrigin(url).origin + path)
    } else {
      if (!opts) {
        opts = typeof url === 'object' ? url : {}
      }

      url = util.parseURL(url)
    }

    const { agent, dispatcher = getGlobalDispatcher() } = opts

    if (agent) {
      throw new InvalidArgumentError('unsupported opts.agent. Did you mean opts.client?')
    }

    return fn.call(dispatcher, {
      ...opts,
      origin: url.origin,
      path: url.search ? `${url.pathname}${url.search}` : url.pathname,
      method: opts.method || (opts.body ? 'PUT' : 'GET')
    }, handler)
  }
}

module.exports.setGlobalDispatcher = setGlobalDispatcher
module.exports.getGlobalDispatcher = getGlobalDispatcher

const fetchImpl = require('undici/lib/web/fetch').fetch

module.exports.fetch = function fetch (init, options = undefined) {
  return fetchImpl(init, options).catch(err => {
    if (err && typeof err === 'object') {
      Error.captureStackTrace(err)
    }
    throw err
  })
}
module.exports.Headers = require('undici/lib/web/fetch/headers').Headers
module.exports.Response = require('undici/lib/web/fetch/response').Response
module.exports.Request = require('undici/lib/web/fetch/request').Request
module.exports.FormData = require('undici/lib/web/fetch/formdata').FormData

const { setGlobalOrigin, getGlobalOrigin } = require('undici/lib/web/fetch/global')

module.exports.setGlobalOrigin = setGlobalOrigin
module.exports.getGlobalOrigin = getGlobalOrigin

const { CacheStorage } = require('undici/lib/web/cache/cachestorage')
const { kConstruct } = require('undici/lib/core/symbols')

module.exports.caches = new CacheStorage(kConstruct)

const { deleteCookie, getCookies, getSetCookies, setCookie, parseCookie } = require('undici/lib/web/cookies')

module.exports.deleteCookie = deleteCookie
module.exports.getCookies = getCookies
module.exports.getSetCookies = getSetCookies
module.exports.setCookie = setCookie
module.exports.parseCookie = parseCookie

const { parseMIMEType, serializeAMimeType } = require('undici/lib/web/fetch/data-url')

module.exports.parseMIMEType = parseMIMEType
module.exports.serializeAMimeType = serializeAMimeType

const { CloseEvent, ErrorEvent, MessageEvent } = require('undici/lib/web/websocket/events')
const { WebSocket, ping } = require('undici/lib/web/websocket/websocket')
module.exports.WebSocket = WebSocket
module.exports.CloseEvent = CloseEvent
module.exports.ErrorEvent = ErrorEvent
module.exports.MessageEvent = MessageEvent
module.exports.ping = ping

module.exports.WebSocketStream = require('undici/lib/web/websocket/stream/websocketstream').WebSocketStream
module.exports.WebSocketError = require('undici/lib/web/websocket/stream/websocketerror').WebSocketError

module.exports.request = makeDispatcher(api.request)
module.exports.stream = makeDispatcher(api.stream)
module.exports.pipeline = makeDispatcher(api.pipeline)
module.exports.connect = makeDispatcher(api.connect)
module.exports.upgrade = makeDispatcher(api.upgrade)

module.exports.MockClient = MockClient
module.exports.MockCallHistory = MockCallHistory
module.exports.MockCallHistoryLog = MockCallHistoryLog
module.exports.MockPool = MockPool
module.exports.MockAgent = MockAgent
module.exports.SnapshotAgent = SnapshotAgent
module.exports.mockErrors = mockErrors

const { EventSource } = require('undici/lib/web/eventsource/eventsource')

module.exports.EventSource = EventSource

function install () {
  globalThis.fetch = module.exports.fetch
  globalThis.Headers = module.exports.Headers
  globalThis.Response = module.exports.Response
  globalThis.Request = module.exports.Request
  globalThis.FormData = module.exports.FormData
  globalThis.WebSocket = module.exports.WebSocket
  globalThis.CloseEvent = module.exports.CloseEvent
  globalThis.ErrorEvent = module.exports.ErrorEvent
  globalThis.MessageEvent = module.exports.MessageEvent
  globalThis.EventSource = module.exports.EventSource
}

module.exports.install = install
